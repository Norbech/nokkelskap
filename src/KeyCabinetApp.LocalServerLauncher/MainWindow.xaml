<Window x:Class="KeyCabinetApp.LocalServerLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeyCabinetApp.LocalServerLauncher"
        mc:Ignorable="d"
        Title="KeyCabinet Server" Height="320" Width="520" WindowStartupLocation="CenterScreen">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="KeyCabinet Server" FontSize="20" FontWeight="SemiBold"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,0" VerticalAlignment="Center">
            <Ellipse x:Name="StatusLamp" Width="14" Height="14" Fill="#B71C1C" Stroke="#40000000" StrokeThickness="1" Margin="0,0,8,0"/>
            <TextBlock x:Name="StatusText" Text="Stoppet" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <Button x:Name="StartStopButton" Content="Start" Height="40" Click="StartStopButton_Click"/>

            <TextBlock Margin="0,12,0,0" TextWrapping="Wrap">
                <Run Text="URL: "/>
                <Hyperlink Click="OpenUrl_Click" NavigateUri="http://127.0.0.1:5000">http://127.0.0.1:5000</Hyperlink>
            </TextBlock>

            <CheckBox x:Name="StopOnCloseCheckBox" Margin="0,10,0,0" IsChecked="True" Content="Stopp server når vinduet lukkes"/>

            <TextBlock x:Name="InfoText" Margin="0,12,0,0" Foreground="#FF555555" TextWrapping="Wrap"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Margin="0,12,0,0" Foreground="#FF777777" FontSize="12" TextWrapping="Wrap">
            Starter Web + HardwareAgent i bakgrunnen uten ekstra vinduer.
        </TextBlock>
    </Grid>
</Window>
